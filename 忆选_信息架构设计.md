# 忆选 · 信息架构（IA）设计文档

> 版本：MVP v1 ｜ 端：Web（PC 顶部常驻导航 / H5 底部 Tab）｜ 角色图例：👤用户 🤖AI ⚙️系统
> 设计基线：用户 18 岁、高焦虑、信息过载、决策犹豫、恨复杂操作、要确定性引导。
> 差异化落到 IA：**双轨记忆**（记忆页）+ **归因白盒**（归因解释卡）。

---

## 一、信息架构原则（对应约束）

1. **扁平优先**：全局仅 4 个一级入口（首页 / 模拟方案 / 记忆 / 我的），页面深度封顶 `首页 > 列表 > 详情`（3 层）。
2. **3 步核心操作**：生成推荐、改偏好、建模拟方案，均 ≤3 次点击触达。
3. **确定性引导**：首页第一屏 = 显眼「开始测评/重新测评」CTA + 「用户画像摘要卡片」，**不投搜索框**（反竞品"扔搜索框"痛点）。
4. **归因白盒高权重**：每条推荐固定携带「归因解释卡」，视觉占位 ≥ 推荐卡的 40%，默认展开。
5. **空状态即引导**：无测评 / 无方案 / 无语义记忆，均给专属引导区，绝不空白页。

---

## 二、信息架构树状图（站点地图）

```
全局导航 Global Nav（PC 顶部 / H5 底部 Tab，常驻）
│
├── 首页 Home
│   ├── 品牌区（Logo + 重新测评入口）
│   ├── 用户画像摘要卡片 ★核心（省份/选科/预估分 + 性格标签 + 完成度）
│   ├── 主 CTA：开始测评 / 重新测评 ★核心（未测评时放大）
│   ├── 快捷入口（查看推荐 → 模拟方案 / 记忆偏好）
│   └── 状态进度条（测评进度 · 方案完整度）
│
├── 测评 Quiz（步骤页 · 局部导航=步骤进度条）
│   ├── 步骤1 基础信息（省份 / 选科 / 预估分 / 位次）
│   ├── 步骤2 性格测评（6-8 题 · 霍兰德简化版）
│   └── 步骤3 生成中（AI 动画 · loading）
│
├── 模拟方案 Plans（列表页 · 局部导航=方案卡片）
│   ├── 方案卡片列表（方案A 610省内 / 方案B 620出省 … 分数假设 + 地域）
│   ├── 新建方案 CTA（一键生成 / 复制改分）
│   └── 空状态引导（暂无方案 → 基于画像一键生成）
│
├── 方案详情 Recommend（详情页 · 局部导航=冲/稳/保 Tab）
│   ├── 方案头部（分数假设 · 地域 · 复制 / 删除）
│   ├── 推荐结果列表（院校+专业 · 冲稳保分组 · 录取概率）
│   ├── 归因解释卡 ★差异化·高视觉权重（逻辑链 + 可展开）
│   └── 操作区（采纳 / 排除 / 微调）
│
├── 记忆 Memory（双轨 · 局部导航=结构化/语义 切换）
│   ├── 结构化记忆（硬约束：分数 · 地域黑名单 · 选科）✎ 可编辑
│   ├── 语义记忆（隐式偏好标签：怕数学 · 喜欢沿海 · 来自对话提取）
│   ├── 偏好覆盖入口（自然语言输入「我不想去东北了」）
│   └── 空状态引导（暂无语义记忆 → 多聊几句我帮你记）
│
└── 我的 Me
    ├── 数据授权说明（成绩本地加密 · 授权才上云）
    ├── 反馈 / 帮助
    └── 关于 / 退出
```

### 跳转关系（→）

| 来源 | 动作 | 去向 |
|------|------|------|
| 首页 | 点主 CTA | → 测评 |
| 首页 | 点快捷入口 | → 模拟方案 / 记忆 |
| 测评（完成） | 自动跳转 | → 方案详情（看首版推荐） |
| 模拟方案 | 点方案卡 | → 方案详情 |
| 模拟方案 | 点新建方案 | → 轻表单（复制改分）→ 方案详情 |
| 方案详情 | 点归因标签「怕数学」 | → 记忆（查看/编辑该偏好） |
| 方案详情 | 点「复制为模拟方案」 | → 模拟方案（新建副本） |
| 记忆（覆盖偏好后） | 提示重算 | → 方案详情（新推荐） |
| 任意页 | 点 Logo / 首页 Tab | → 首页 |

---

## 三、关键页面信息清单（5 个核心页面）

### 3.1 首页 Home
| 核心模块/区块 | 承载的用户任务 | 数据来源 | 交互反馈 |
|------|------|------|------|
| 用户画像摘要卡片 ★ | 一眼确认"我是谁/当前状态" | AI生成(画像)+用户输入(基础信息) | 卡片实时展示；未完成显示进度缺口提示 |
| 主 CTA 开始测评/重新测评 ★ | 启动或重做测评 | ⚙️系统 | 按钮高亮+点击动效，进入测评页 |
| 快捷入口 | 直达推荐/方案/记忆 | ⚙️系统 | 角标显示未完成项数量 |
| 状态进度条 | 感知整体进度·降焦虑 | ⚙️系统 | 测评/方案完成度可视化 |
| 空状态引导（未测评） | 引导首次测评 | ⚙️系统 | 大按钮 + 一句话价值主张（非搜索框） |

### 3.2 测评页 Quiz
| 核心模块/区块 | 承载的用户任务 | 数据来源 | 交互反馈 |
|------|------|------|------|
| 步骤进度条 | 感知进度·降低半途放弃 | ⚙️系统 | 「第 X / 8 题」高亮 + 防卡死 |
| 基础信息表单 | 录入省份/选科/预估分 | 👤用户输入 | 实时校验 + 草稿自动保存（中断可续） |
| 性格测评题（6-8题） | 完成霍兰德简化测评 | 👤用户输入 | 选项即时高亮；作答过快/一致性低→提示重测 |
| 生成中动画 | 等待 AI 出画像 | 🤖AI | loading + 预计耗时，避免"卡住"错觉 |

### 3.3 模拟方案列表页 Plans
| 核心模块/区块 | 承载的用户任务 | 数据来源 | 交互反馈 |
|------|------|------|------|
| 方案卡片列表 | 查看/对比多版本（A/B…） | ⚙️系统(副本)+👤(分数假设) | 卡片显分数/地域/完整度，支持并列对比 |
| 新建方案 CTA | 创建模拟（复制改分） | ⚙️系统 | 轻表单弹出，秒级建副本 |
| 空状态引导（无方案） | 引导首份方案 | ⚙️系统 | 「基于画像一键生成」按钮，零门槛 |

### 3.4 方案详情 / 推荐页 Recommend
| 核心模块/区块 | 承载的用户任务 | 数据来源 | 交互反馈 |
|------|------|------|------|
| 方案头部 | 确认本版本假设（分数/地域） | 👤输入+⚙️系统 | 显示假设；可复制/删除 |
| 推荐结果列表（冲稳保） | 浏览院校+专业 | 🤖AI生成 | 分组标签 + 录取概率，梯度清晰 |
| 归因解释卡 ★ | 理解"为什么推荐"（白盒） | 🤖AI（逻辑链） | 彩色逻辑链 + 可展开；占卡 ≥40% 视觉权重 |
| 操作区（采纳/排除/微调） | 实时调整方案 | 👤用户输入 | 即时重排 + 自动保存 |

### 3.5 记忆页 Memory
| 核心模块/区块 | 承载的用户任务 | 数据来源 | 交互反馈 |
|------|------|------|------|
| 结构化记忆（硬约束） | 管理分数/地域黑名单/选科 | 👤输入/⚙️系统(JSON) | 可编辑；改动后提示「将重算推荐」 |
| 语义记忆（隐式偏好） | 查看对话提取的偏好 | 🤖AI提取(向量库) | 标签云 + 显示来源对话片段 |
| 偏好覆盖入口 | 自然语言改偏好（如不去东北） | 👤输入→🤖AI | 解析成功 Toast + 写入标签 + 触发重算 |
| 空状态引导（无语义记忆） | 引导多对话积累 | ⚙️系统 | 「多聊几句，我帮你记住」提示 |

---

## 四、核心操作路径（User Flow Mapping）

### 场景 A：新用户首次进入 → 完成测评 → 看到首版推荐
```
1. [👤] 打开首页 → 见空状态引导（大CTA「开始测评」+ 价值一句话，无搜索框）
2. [👤] 点主CTA → 进入测评页步骤1
3. [👤] 填省份/选科/预估分 → [⚙️] 实时校验+草稿保存，进度条推进
4. [👤] 作答 6-8 题性格测评 → [⚙️] 进度条「第X/8题」，过快则提示
5. [⚙️] 提交 → [🤖] loading 生成「分数+性格」画像（2-3s 动画）
6. [🤖] 画像就绪 → 自动跳转方案详情
7. [🤖] 基于画像+硬约束生成首版推荐（冲/稳/保）+ 归因解释卡
8. [👤] 看到首版推荐与归因 → 可采纳/排除/微调（核心操作 ≤3 步达成）
```

### 场景 B：老用户修改偏好（"我怕数学"）→ 系统重推荐
```
1. [👤] 在方案详情或记忆页 输入自然语言「我怕数学」
2. [⚙️] 路由到记忆页语义覆盖入口（或对话浮窗）
3. [🤖] 意图解析 → 写入语义记忆(向量库)「怕数学」标签，补充/覆盖旧偏好
4. [🤖] 触发重算 → 重新匹配，排除强数学要求的专业
5. [⚙️] 推送「已更新偏好，重新生成推荐」提示
6. [👤] 查看新推荐 → 归因卡显示「因您说怕数学，已排除 XX 专业」
7. [👤] 确认/继续微调（白盒可见，消除黑盒焦虑）
```

### 场景 C：用户创建模拟方案（复制当前方案改分数）
```
1. [👤] 在方案详情/列表 点「复制为模拟方案」
2. [⚙️] 弹出轻量表单：新分数假设(如620) + 命名(方案B) + 地域(出省)
3. [👤] 输入 620 分、选「出省」→ 确认
4. [⚙️] 创建隔离副本（独立记忆空间，继承基础硬约束）
5. [🤖] 基于新分数 + 该版本记忆 生成方案B推荐 + 归因卡
6. [👤] 在方案列表见 方案A(610省内) / 方案B(620出省) 并列对比
7. [👤] 切换/对比/采纳任一版本（多版本记忆隔离，互不污染）
```

---

## 五、差异化功能在 IA 中的落点

| 差异化 | IA 落点 | 视觉/交互处理 |
|------|------|------|
| 双轨记忆 | 独立「记忆」一级入口，分结构化/语义双栏 | 结构化可编辑表单；语义用标签云+来源对话 |
| 归因白盒 | 每条推荐固定「归因解释卡」 | 占用推荐卡 ≥40%，彩色逻辑链，默认展开，可点标签跳记忆 |
| 反竞品黑盒 | 首页不投搜索框，首屏给画像+测评 | 强引导而非强搜索，降低信息过载 |

---

## 六、空状态设计清单（专项）

| 页面 | 空状态触发 | 引导区设计 |
|------|------|------|
| 首页 | 未测评 | 大 CTA「开始测评」+ 一句话价值主张，禁搜索框 |
| 模拟方案 | 无方案 | 「基于画像一键生成首份方案」按钮 |
| 方案详情 | 无推荐结果 | 「放宽条件 / 调整分数」引导，而非空白 |
| 记忆·语义 | 无隐式偏好 | 「多聊几句，我帮你记住」对话引导 |
| 推荐·排除殆尽 | 全排除 | 「重置偏好 / 复制新方案」提示 |
